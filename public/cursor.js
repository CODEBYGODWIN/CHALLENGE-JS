// Sélectionnez l'élément HTML sur lequel vous souhaitez appliquer le curseur personnalisé
const element = document.querySelector('body');

// Modifiez le style du curseur en utilisant l'image personnalisée
element.style.cursor = 'url(./cursor.png), auto';

